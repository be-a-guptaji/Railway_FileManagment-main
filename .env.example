# Environment Variables for Railway File Management System
# Copy this file to .env and update the values for local development
# For Railway deployment, set these variables in the Railway dashboard

# ===== REQUIRED VARIABLES =====

# Database Configuration (Railway provides this automatically)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Flask Security (IMPORTANT: Generate a secure key for production)
SECRET_KEY=your-super-secret-key-here-generate-a-secure-one

# ===== OPTIONAL VARIABLES =====

# Flask Environment (Railway sets this automatically)
FLASK_ENV=production

# Port (Railway sets this automatically)
PORT=5000

# Upload Configuration
UPLOAD_FOLDER=uploads

# ===== ADMIN USERS CONFIGURATION =====
# Change these default credentials for security!

ADMIN_USER_1=your_admin_username
ADMIN_PASS_1=your_secure_password

ADMIN_USER_2=your_second_admin_username
ADMIN_PASS_2=your_second_secure_password

# ===== RAILWAY DEPLOYMENT INSTRUCTIONS =====
# 1. Create a new Railway project
# 2. Connect your GitHub repository
# 3. Add a PostgreSQL database service
# 4. Set the following environment variables in Railway dashboard:
#    - SECRET_KEY (generate a secure random string)
#    - ADMIN_USER_1 (your admin username)
#    - ADMIN_PASS_1 (your secure admin password)
#    - ADMIN_USER_2 (optional second admin)
#    - ADMIN_PASS_2 (optional second admin password)
# 5. Deploy!

# ===== SECURITY NOTES =====
# - Never commit .env file to version control
# - Use strong, unique passwords for admin accounts
# - Generate a cryptographically secure SECRET_KEY
# - DATABASE_URL is automatically provided by Railway